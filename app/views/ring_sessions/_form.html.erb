<%= form_for(:session, url: ring_sessions_path) do |f| %>

<div class="field">
	<%= f.label :latitude %>
	<%= f.text_field :latitude%>
	<%= f.label :longitude %>
	<%= f.text_field :longitude%>
</div>

<div class="actions">
	<%= f.submit %>
</div>

<% end %>